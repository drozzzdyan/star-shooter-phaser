import Btn from "./btn.js";

export default class btnShot extends Btn {
  constructor(scene, x, y, name, frameStatic, frameActive, textInner) {
    super(scene, x, y, name, frameStatic, frameActive, textInner);
  }

  
}